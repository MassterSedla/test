spring:
  docker:
    compose:
      enabled: true
      lifecycle-management: start_and_stop
  jpa:
    show-sql: true
    hibernate.ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://test-db:33066/test
    username: test
    password: test